{
  "name": "@mai/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply mai-db --local",
    "migrate:remote": "wrangler d1 migrations apply mai-db --remote"
  },
  "dependencies": {
    "@mai/shared": "*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.99.0"
  }
}
